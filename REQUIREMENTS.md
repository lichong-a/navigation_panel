# 导航面板网站需求文档

## 项目概述

一个简洁美观的个人导航网站，充满动效和3D效果，用于快速导航到常用网站。

---

## 功能需求

### 1. 核心展示功能

#### 1.1 导航卡片
- **分组展示**：网站按分组排列
- **卡片内容**：网站图标 + 标题
- **点击行为**：跳转到目标网站

#### 1.2 内网/外网模式切换
- 位置：页面右上角
- 功能：全局切换所有链接的内网/外网地址
- 状态记忆：刷新后保持用户选择

#### 1.3 页面动效
- 滚轮事件交互
- 3D 视觉效果
- 页面过渡动画
- 卡片悬停动效

---

### 2. 管理功能（需登录）

#### 2.1 认证系统
- **首次启动向导**：检测到无 config.json 时进入初始化向导，设置管理员用户名和密码
- **登录方式**：用户名 + 密码
- **会话管理**：JWT Token
- **修改密码**：登录后可修改用户名和密码

#### 2.2 数据管理
- **导出配置**：导出 sites.json 文件
- **导入配置**：导入 sites.json 文件（覆盖或合并）
- **导出全部**：打包导出 sites.json + uploads/ 图标文件夹

#### 2.3 分组管理
- 创建分组
- 编辑分组名称
- 删除分组
- 分组排序（拖拽）

#### 2.3 网站管理
- 创建网站
- 编辑网站
- 删除网站
- **网站排序（拖拽）**

每个网站卡片包含以下字段：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 标题 | string | ✅ | 网站显示名称 |
| 描述 | string | ❌ | 网站描述（悬停显示） |
| 图标 | string/ file | ✅ | 网站图标 |
| 公网地址 | string | ✅ | 外网访问地址 |
| 内网地址 | string | ❌ | 内网访问地址 |
| 打开方式 | enum | ✅ | 见下方 |
| 标签 | string[] | ❌ | 用于筛选/搜索 |
| 是否启用 | boolean | ❌ | 禁用后不显示 |

**自动字段（不可编辑）：**
- `order` - 排序权重（拖拽自动更新）
- `createdAt` - 创建时间
- `updatedAt` - 更新时间

**打开方式选项：**
1. `current` - 当前页面打开
2. `blank` - 新窗口打开
3. `modal` - 当前页面弹窗（iframe）打开

#### 2.4 图标获取方式

1. **主动上传**：上传图片文件（PNG/ICO/SVG）
2. **自动获取**：输入URL后自动抓取 favicon
3. **Iconify 搜索**：搜索并选择 Iconify 图标库

---

## 技术栈推荐

### 前端

| 技术 | 版本 | 用途 |
|------|------|------|
| **React** | v19.x | 前端框架 |
| **Next.js** | v15.x | 全栈框架（App Router） |
| **TypeScript** | v5.x | 类型安全 |
| **Tailwind CSS** | v4.x | 样式框架 |
| **Three.js** | v0.183+ | 3D 效果 |
| **Framer Motion** | v12.x | 动画库 |
| **Iconify** | v6.x | 图标系统 |
| **Zustand** | v5.x | 状态管理 |

### 后端（Next.js App Router）

| 技术 | 用途 |
|------|------|
| **Route Handlers** | API 接口 |
| **Server Actions** | 表单处理 |
| **JWT** | 身份认证 |
| **bcrypt** | 密码加密 |

### 数据持久化

| 方案 | 说明 |
|------|------|
| **JSON 文件存储** | 轻量级，无需数据库 |
| 文件路径 | `./data/` 目录下 |
| 配置文件 | `config.json`（管理员信息） |
| 数据文件 | `sites.json`（分组和网站数据） |

### 部署

```bash
# 安装依赖
pnpm install

# 构建
pnpm build

# 启动（单进程）
pnpm start

# 或使用 PM2
pm2 start pnpm --name nav-panel -- start
```

---

## 数据结构设计

### config.json（动态生成）

```json
{
  "initialized": true,
  "admin": {
    "username": "admin",
    "passwordHash": "$2b$10$..."
  },
  "jwtSecret": "随机生成的密钥",
  "createdAt": 1709000000000
}
```

### sites.json

```json
{
  "groups": [
    {
      "id": "uuid-v4",
      "name": "常用工具",
      "order": 1,
      "createdAt": 1709000000000,
      "updatedAt": 1709000000000
    }
  ],
  "sites": [
    {
      "id": "uuid-v4",
      "groupId": "uuid-v4",
      "title": "GitHub",
      "description": "代码托管平台",
      "icon": {
        "type": "iconify | uploaded | favicon",
        "value": "mdi:github"
      },
      "publicUrl": "https://github.com",
      "privateUrl": "http://192.168.1.100:3000",
      "openMode": "blank",
      "tags": ["dev", "git"],
      "order": 1,
      "enabled": true,
      "createdAt": 1709000000000,
      "updatedAt": 1709000000000
    }
  ]
}
```

---

## API 设计

### 公开接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/login` | 管理员登录 |
| POST | `/api/auth/logout` | 退出登录 |
| GET | `/api/sites` | 获取所有分组和网站 |

### 管理接口（需认证）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/groups` | 获取分组列表 |
| POST | `/api/admin/groups` | 创建分组 |
| PUT | `/api/admin/groups/:id` | 更新分组 |
| DELETE | `/api/admin/groups/:id` | 删除分组 |
| GET | `/api/admin/sites` | 获取网站列表 |
| POST | `/api/admin/sites` | 创建网站 |
| PUT | `/api/admin/sites/:id` | 更新网站 |
| DELETE | `/api/admin/sites/:id` | 删除网站 |
| POST | `/api/admin/upload` | 上传图标 |
| GET | `/api/admin/favicon` | 获取网站 favicon |
| GET | `/api/admin/iconify/search` | 搜索 Iconify 图标 |

### 系统管理接口（需认证）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/admin/account` | 获取当前账户信息 |
| PUT | `/api/admin/account` | 修改用户名和密码 |
| GET | `/api/admin/export` | 导出 sites.json |
| POST | `/api/admin/import` | 导入 sites.json |
| GET | `/api/admin/export-all` | 打包导出（含图标） |

### 初始化接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/setup/status` | 检查是否已初始化 |
| POST | `/api/setup/init` | 执行初始化（设置管理员） |

---

## 页面结构

### 初始化向导（首次启动）

```
┌─────────────────────────────────────────────────┐
│                                                 │
│              🚀 欢迎使用导航面板                  │
│                                                 │
│         请设置管理员账户以继续                    │
│                                                 │
│         ┌─────────────────────────┐             │
│         │ 用户名                  │             │
│         └─────────────────────────┘             │
│         ┌─────────────────────────┐             │
│         │ 密码                    │             │
│         └─────────────────────────┘             │
│         ┌─────────────────────────┐             │
│         │ 确认密码                │             │
│         └─────────────────────────┘             │
│                                                 │
│              [ 完成设置 ]                        │
│                                                 │
└─────────────────────────────────────────────────┘
```

### 主页面
│  Logo                    [内网/外网] [登录/管理] │
├─────────────────────────────────────────────────┤
│                                                 │
│   ┌─────────────────────────────────────────┐   │
│   │  分组 1                                   │   │
│   │  ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐         │   │
│   │  │ 📁│ │ 📁│ │ 📁│ │ 📁│ │ 📁│         │   │
│   │  │标题│ │标题│ │标题│ │标题│ │标题│        │   │
│   │  └───┘ └───┘ └───┘ └───┘ └───┘         │   │
│   └─────────────────────────────────────────┘   │
│                                                 │
│   ┌─────────────────────────────────────────┐   │
│   │  分组 2                                   │   │
│   │  ┌───┐ ┌───┐ ┌───┐                      │   │
│   │  │ 📁│ │ 📁│ │ 📁│                      │   │
│   │  │标题│ │标题│ │标题│                     │   │
│   │  └───┘ └───┘ └───┘                      │   │
│   └─────────────────────────────────────────┘   │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

## 视觉效果设计

### 动效清单

1. **入场动画**：卡片依次淡入 + 上浮
2. **悬停效果**：卡片 3D 倾斜 + 发光边框
3. **点击反馈**：缩放 + 涟漪效果
4. **滚动视差**：背景层与前景层差速滚动
5. **分组切换**：平滑过渡 + 折叠动画
6. **模式切换**：内网/外网切换时的颜色过渡

### 3D 效果

- 背景：Three.js 粒子系统或低多边形地形
- 卡片：CSS 3D transform 实现透视倾斜
- 可选：浮动几何体装饰

---

## 开发优先级

### Phase 1: 基础功能
- [ ] 项目初始化
- [ ] 基础布局和样式
- [ ] 数据存储层
- [ ] 公开 API
- [ ] 卡片展示

### Phase 2: 认证与管理
- [ ] 管理员登录
- [ ] 分组 CRUD
- [ ] 网站 CRUD
- [ ] 图标上传

### Phase 3: 动效增强
- [ ] 入场动画
- [ ] 悬停效果
- [ ] Three.js 背景

### Phase 4: 优化打磨
- [ ] 响应式适配
- [ ] 性能优化
- [ ] 部署脚本

---

## 环境要求

- Node.js >= 24.x
- pnpm >= 10.x
